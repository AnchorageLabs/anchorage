{
  "name": "@anchorage/agent-deploy-watch",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "bin": {
    "deploy-watch": "./dist/index.js"
  },
  "files": ["agent.json", "dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@anchorage/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.6.2"
  }
}
